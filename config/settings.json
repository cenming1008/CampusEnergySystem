{
  "_comment": "设备告警阈值配置。default 为全局默认值，device_thresholds 为按设备 ID 覆盖；未配置设备覆盖时使用 default。",
  "default": {
    "voltage_min": 190.0,
    "voltage_max": 250.0,
    "current_max": 45.0
  },
  "device_thresholds": {}
}

{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        { "expr": "sum(rate(mine_mqtt_messages_total[5m])) by (status)", "legendFormat": "{{status}}", "refId": "A" }
      ],
      "title": "MQTT Throughput by Status",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(mine_mqtt_processing_duration_seconds_bucket[5m])) by (le, status))", "legendFormat": "{{status}}", "refId": "A" }
      ],
      "title": "MQTT P95 Processing Latency",
      "type": "timeseries"
    },
    {
      "datasource": "Loki",
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "options": { "dedupStrategy": "none", "enableLogDetails": true, "showLabels": true, "sortOrder": "Descending" },
      "targets": [
        { "expr": "{service=\"mqtt\"} |= ``", "queryType": "range", "refId": "A" }
      ],
      "title": "MQTT Broker Logs",
      "type": "logs"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["mine", "mqtt", "observability"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Campus Energy System MQTT Observability",
  "version": 1
}

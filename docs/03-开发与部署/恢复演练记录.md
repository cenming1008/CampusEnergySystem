# 恢复演练记录

## 2026-03-26

- 演练环境：`mine_energy_db_dev`
- 目标：执行一次完整的 `backup -> restore -> verify`
- 备份标签：`restore_drill_custom_20260326`

### 执行步骤

```bash
DB_CONTAINER_OVERRIDE=mine_energy_db_dev bash ./scripts/shell/backup.sh --label restore_drill_custom_20260326
DB_CONTAINER_OVERRIDE=mine_energy_db_dev bash ./scripts/shell/restore.sh --yes backups/latest_restore_drill_custom_20260326.dump
docker exec mine_energy_db_dev psql -U admin -d mine_energy -tAc "select count(*) from information_schema.tables where table_schema='public';"
docker exec mine_energy_db_dev psql -U admin -d mine_energy -tAc "select extname from pg_extension where extname='timescaledb';"
docker exec mine_energy_db_dev psql -U admin -d mine_energy -tAc "select count(*) from public.energydata;"
```

### 验证结果

- 备份文件生成成功
- 恢复脚本执行成功
- 恢复后数据库 `public` schema 表数量为 `21`
- TimescaleDB 扩展存在：`timescaledb`
- `public.energydata` 数据量校验通过：`81644`

### 备注

- 本次演练使用当前开发数据库同库备份/回灌，目标是验证恢复流程本身可执行
- 已将备份默认格式切换为 `pg_dump -Fc` 自定义格式，以解决 TimescaleDB 旧版 SQL 备份回灌失败问题
- 正式工业上线前，仍应在预生产或隔离环境再做一次带业务校验的恢复演练

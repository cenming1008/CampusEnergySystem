# 紧急修复 - 重启服务

## 问题原因

**后端服务停止了**，导致前端无法连接到API，返回404错误。

## ✅ 立即执行以下步骤

### 1️⃣ 重启后端服务（最重要！）

找到运行后端的终端（Terminal 19），执行：

```bash
cd /Users/todo/MineEnergySystem
python3 run.py
```

等待看到：
```
✨ 系统就绪 - 煤矿综合能源管理系统 v2.0.0
INFO:     Uvicorn running on http://127.0.0.1:8088
```

### 2️⃣ 验证后端启动成功

打开新终端，测试：
```bash
curl http://localhost:8088/health
```

应该返回：
```json
{"status":"healthy",...}
```

### 3️⃣ 刷新浏览器

访问：http://localhost:3000

应该能正常打开登录页面了。

---

## 🔍 如果还是404

### 检查1: 前端服务是否运行

```bash
lsof -i :3000
```

如果没有输出，说明前端也停了，需要重启：

```bash
cd /Users/todo/MineEnergySystem/frontend
npm run dev
```

### 检查2: 端口是否正确

确保访问的是：
- ✅ http://localhost:3000
- ❌ 不是 http://localhost:3001

### 检查3: 后端日志

如果后端启动失败，查看错误信息并告诉我。

---

## 📝 我做的修改

**前端**：`frontend/src/views/EnergyManagement.vue`
- 添加了详细日志
- 增强了错误处理
- ✅ 代码没有语法错误

**后端**：`app/services/energy_service.py`
- 修复了缺少 `avg_consumption` 字段的问题
- ✅ 代码没有语法错误

---

## 🎯 快速恢复清单

- [ ] 后端运行在 8088 端口
- [ ] 前端运行在 3000 端口
- [ ] `curl http://localhost:8088/health` 返回 healthy
- [ ] 浏览器能打开 http://localhost:3000
- [ ] 能正常登录系统

---

**现在请立即重启后端服务！** 🚀

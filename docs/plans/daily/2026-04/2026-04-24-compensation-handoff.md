# 2026-04-24 Compensation Handoff

## 当前主题
- `设备监控页实时数据语义收敛专题` 的补偿器控制链路 UAT 打磨

## 当前结论
- 补偿器控制链路已完成一轮生产前打磨：控制回执落库后可主动推送前端刷新，控制链路日志可追踪，JKWF 解码异常可观测，回执超时可配置，SQLite 写参风险有启动提示。
- 当前不改变既有前端主交互：控制台仍保留 2 秒 / 5 秒轮询，WebSocket 事件只作为更快刷新触发。
- 当前不改变正式完善门槛：真实设备/网关联调前，仍不能把模拟回执口径认定为协议冻结。

## 下一棒
- 交给验收角色：
  - 确认“控制回执实时事件 + 数据库控制日志 + 前端轮询兜底”的开放边界是否满足 UAT。
  - 确认 `COMPENSATION_CONTROL_RECEIPT_TIMEOUT_SECONDS=120` 的默认值是否可进入现场试运行。
- 交给设备/网关联调角色：
  - 用真实设备或真实网关验证回执 topic、payload、`command_id` 关联键和 `running / success / failed / timeout / rejected` 状态语义。
  - 验证真实参数写入和远程动作的成功、失败、拒绝、超时路径是否都能稳定落库并推送前端刷新。
  - 确认生产部署是否存在多 API 实例并发写参；若存在，需要新开 Redis/数据库级分布式锁方案。

## 禁止扩张
- 不把本轮 WebSocket 事件升级为公开第三方 API 合约。
- 不在真实设备联调前宣布补偿器控制协议冻结。
- 不在未确认生产数据库和部署形态前引入 Redis 分布式锁或大范围控制服务重构。

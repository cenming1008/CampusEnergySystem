# 2026-04-23 Handoff

## 当前主题
- LSTM / Forecast 删除后的环境风险收口

## 当前结论
- 旧预测模块相关代码入口、依赖链与构建产物已完成清理，当前运行、测试、迁移与前端构建均不再依赖 `forecast` / `lstm_forecast` / `data_generator`。
- 开发库、`venv`、前端 `dist` 已完成重建，迁移链对“`device_subtype` 已存在”与“`prediction` 表已不存在”场景已具备幂等兼容。
- 历史计划文档中的 `Forecast` / `LSTM` 记录保留为历史背景，不属于本轮需要继续删除的残留。

## 下一棒
- 交给验收角色：
  - 按“环境风险收口”口径确认本轮通过，不再把历史文档引用误判为可执行旧入口残留
  - 若需要切换主主题，再由规则/验收统一决定主区是否改写
- 交给后续维护角色：
  - 若再新增 README、结构说明或脚本文档，保持与当前仓库实际文件一致，不恢复 `forecast.ts`、`Forecast.vue` 等已删除入口

## 禁止扩张
- 不把本轮文档清理扩成历史 PLAN 批量改写或 archive 大规模重写。
- 不因历史记录中出现 `Forecast` / `LSTM` 就恢复预测模块占位入口。
- 不在没有新立项的前提下，顺手重建新的预测页面、预测接口或训练脚本。

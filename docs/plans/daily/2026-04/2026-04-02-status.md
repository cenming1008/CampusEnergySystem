# 2026-04-02 Status

## 当前总目标
- 将当前主主题保持为“MQTT 采集进程解耦专题”，完成第二阶段“worker / bridge 细粒度观测 + replay / 补偿链路验证”的最小闭环验收，并进入阶段收口判断。

## 当前阶段
- 已完成探索、规范、后端与验收。
- 当前结论：第二阶段最小闭环已成立，可进入阶段收口。

## 当前验证结论
- API 已脱离内嵌 MQTT 消费，只保留 Redis bridge 订阅与 WebSocket 广播。
- 独立 worker 已可单独启动。
- `/health` 已能同时区分 `mqtt_worker`、`mqtt_bridge`、`api_realtime`。
- worker / bridge / API 三段状态已具备更细粒度 `meta` 与最近一次关键阶段状态。
- replay / 补偿脚本已输出可机器读取的 JSON 摘要，并区分 `replayed / failed_* / skipped_*`。
- `python3 -m compileall ...` 通过。
- `PYTHONPATH=. venv/bin/pytest -q tests/test_runtime_controls.py tests/test_mqtt_realtime_bridge.py tests/test_health_endpoint.py tests/test_replay_mqtt_failures.py tests/test_monitoring_access.py tests/test_websocket_auth.py tests/test_mqtt_reliability_service.py` 结果为 `28 passed`。

## 当前剩余风险
- 当前仍停留在“独立 worker”架构增强阶段，不等于完整 ingest service / 完整观测体系已完成。
- 第二阶段验证仍以单测与脚本输出为主，尚未做真实三进程长链路演练。

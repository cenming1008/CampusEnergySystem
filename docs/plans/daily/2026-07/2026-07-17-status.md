# 2026-07-17 园区光储 Task 3 状态快照

## 当天目标

- 按 Superpowers 实施计划和 TDD 完成储能资产、扩展遥测与调度计划持久化契约。

## 当天进展

- 完成 `StorageAssetProfile`、`StorageDispatchPlan` 和八个批准的 `StorageTelemetry` 扩展。
- 新增静态、offline-safe 的 `20260716_0002` migration；未重建根基线已有的 `storage_telemetry`。
- 调整根基线护栏，使其继续严格校验 `0001` 基线表，同时允许单独验收批准的 additive revision。
- Task 3 实现提交为 `aa97e3ad`。

## 当天验证

- migration 与 model 测试均先观察到计划内 RED，再恢复 GREEN。
- offline SQL 生成通过。
- fresh、offline、roundtrip 各 682 个 schema objects，共同指纹为 `b81c0db6aaef07fad85a7b617b005e99e1aaafee382e06da6f378eea6d4cbaec`。
- 三条路径均到 `20260716_0002`，均保留 `public.energydata` hypertable，并包含两张新增表和八个批准列。
- 全量后端 `740 passed, 5 warnings`；Task 3 Ruff 通过。
- 实际开发库只读核对仍为 `20260716_0001`，两张 Task 3 表尚未应用；本轮未修改开发库。

## 收口判断

- Task 3：通过并正式完成。
- Task 4：解除持久化依赖，下一轮按 TDD 接入扩展仿真遥测。
- 进入依赖新结构的真实联调前，必须显式升级并复核开发库到 `20260716_0002`。

## 园区光储 Task 4 状态快照

- Task 4 实现提交：`0e1fd1bc`。
- 完成扩展仿真遥测入库、三个设备 state 键到持久化 status 列的映射，以及监控聚合指标。
- RED 由新字段未提取和监控指标不存在触发；实现后 focused、治理文档测试和全量回归恢复 GREEN。
- 完整环境全量后端 `742 passed, 5 warnings`；Task 4 Ruff 通过。
- Task 4 通过并正式完成；Task 5 解除依赖，尚未开始。

## 园区光储 Task 5 状态快照

- 新增 500 kWh/250 kW 储能 MQTT 系统级仿真器，支持五个固定场景、固定 seed、加速时钟、双控制主题、模拟回执和优雅停止。
- `--print-only` 不创建网络客户端，直接文档命令可输出强制带 `data_source=simulated` 的合法 JSON。
- 储能 EMS 与应用内仿真开关在 Python 设置及三份环境模板中均默认关闭。
- RED 由缺失模块触发；直接入口导入问题由独立子进程回归测试复现后修复。
- 聚焦测试 `12 passed`，完整环境全量后端 `754 passed, 7 warnings`；Ruff 与差异检查通过。
- Task 5 通过并正式完成；Task 6 解除依赖，尚未开始。

## 单一储能系统计划收敛快照

- 用户确认只保留一个储能业务系统；模拟器与未来厂商网关作为同一标准 MQTT 契约的可替换数据源。
- Task 1-5 完成状态和实现全部保留，不推倒重做。
- 原有 `StorageMonitorView` 继续作为唯一设备级监控/控制页面；园区级能力进入现有 `EnergyManagement` 的“光储 EMS”工作区。
- 后续计划补充设备级自动控制默认关闭、模拟运行标识、按设备清理预览和适配器替换验收。
- 当前下一任务仍为后端储能 Task 6，没有并行第二主题。

## 园区光储 Task 6 状态快照

- 按 TDD 新增储能命令生命周期、类别感知 MQTT 回执分发与独立超时收敛任务。
- 控制日志固定使用 `storage-control-api`，结构化记录命令上下文、`data_source` 与可选 `simulation_run_id`。
- 保留旧 MQTT processor 导出与电容补偿回执路径；终态回执具备幂等与迟到冲突保护。
- 聚焦与兼容回归 `40 passed, 2 warnings`；完整后端 `767 passed, 2 skipped, 7 warnings`。
- Task 6 通过并正式完成；Task 7 是唯一下一棒。

## 园区光储 Task 7 状态快照

- 新增 `20260717_0003`，只增加设备级自动授权、两处模拟运行标识和调度计划来源，共四列两索引。
- fresh、offline、roundtrip 均为 688 个对象；临时数据库已按固定名单清理。
- 原有储能嵌套边界增加 profile、control 和 simulation API；模拟控制默认关闭并与生产 topic 隔离。
- 自动授权仅管理员可改，启用前要求已有档案、新鲜遥测、BMS 正常、PCS 可用和并网。
- 完整后端 `783 passed, 2 skipped, 7 warnings`；Task 7 通过，Task 8 成为唯一下一棒。

## 园区光储 Task 8 状态快照

- 新增安全优先纯规则，固定 safety、光伏消纳、需量、电价、idle 优先级和正充负放符号。
- 增加死区、SOC/温度滞回、最小启停、换向待机及非有限输入保护。
- EMS 只在全局/设备双门禁、auto 模式、无 pending、遥测完整新鲜且目标差值越过死区时复用 Task 6 下发。
- 60 秒任务默认不注册；单设备异常独立回滚。
- 完整后端 `801 passed, 2 skipped, 7 warnings`；Task 8 通过，Task 9 成为唯一下一棒。

## 园区光储 Task 9 状态快照

- 实现提交：`6254dd8a`。
- 完成 `accepted -> running -> success` 模拟执行状态机；功率连续三个步长满足固定容差后才成功。
- 完成 SOC/温度/PCS 安全拒绝、通信中断超时注入、手动/自动模式、停止和终态幂等复发。
- 每个模拟器实例生成稳定 UUID，所有模拟遥测与回执携带同一 `simulation_run_id` 和 `data_source=simulated`。
- 场景、速度和固定故障只接受 simulation topic，且 `STORAGE_SIMULATION_ENABLED` 默认关闭；真实控制 topic 拒绝 simulator-only 动作。
- 聚焦测试 `19 passed`，完整后端 `811 passed, 2 skipped, 7 warnings`；Ruff 与差异检查通过。
- Task 9 通过并正式完成；Task 10 成为唯一下一棒。

## 园区光储 Task 10 状态快照

- 原有 `StorageMonitorView` 已扩展为统一储能工作台，未新增模拟版页面或路由。
- 数据来源、目标/实际/可用功率、安全状态、权限门禁、人工控制和命令时间线已落地；accepted 不等于执行成功。
- 实现提交 `a83cbc49`；相关回归 `27 passed`，typecheck、build 和变更文件 ESLint 通过。
- 前端全量仍有 4 项既有基线失败：`EnergyManagement` 三项、`DeviceTrendPanel` 一项。
- Task 10 通过并正式完成；Task 11 日前 MILP 优化器成为唯一下一棒。

# 2026-07-17 园区光储 Task 3 交接快照

## 已完成

- Task 3 提交：`aa97e3ad`。
- 持久化 revision：`20260716_0002`，父 revision：`20260716_0001`。
- 新增 `storage_asset_profile`、`storage_dispatch_plan`，并为 `storage_telemetry` 增加八个批准扩展。

## 下一棒：后端储能 Task 4

- 先写仿真 payload 入库与 monitor 聚合测试并观察 RED。
- MQTT 使用 `bms_state`、`pcs_state`、`grid_connection_state`；入库映射到 `bms_status`、`pcs_status`、`grid_status`。
- 保持负功率表示放电，保留 `data_source=simulated`。
- 实际开发库仍为 `20260716_0001`；真实联调前显式升级到 `20260716_0002` 并复核，不允许 runtime metadata 隐式补表。
- 本轮不提前展开仿真 CLI、控制闭环或优化调度。

## 验收证据

- 全量后端 `740 passed, 5 warnings`。
- 三条真实 TimescaleDB migration 路径均为 682 个对象且指纹一致。
- Task 3 Ruff 通过；远端 GitHub Actions 本轮未运行。

## 园区光储 Task 4 交接

- Task 4 提交：`0e1fd1bc`。
- 仿真扩展遥测入库与监控聚合已完成，负功率放电符号及 `data_source=simulated` 均有测试固定。
- 下一棒为后端储能 Task 5：先按 TDD 构建可复用 MQTT 仿真器 CLI、确定性输出和默认关闭设置。
- 正常 MQTT 联调前必须显式升级开发库到 `20260716_0002`；`--print-only` 不依赖数据库。

## 园区光储 Task 5 交接

- Task 5 已完成可复用 MQTT 储能仿真器、确定性场景、默认关闭门禁和入口文档。
- 下一棒为后端储能 Task 6：按 TDD 实现储能命令 lifecycle、category-aware 回执 dispatcher 和超时收敛。
- 必须保留正充负放、模拟来源标记、电容补偿回执兼容路径，以及“入队不等于执行成功”的状态边界。
- 真实 MQTT/数据库联调前仍需显式升级并复核 `campus_energy` 到 `20260716_0002`。

## 单一储能系统计划收敛交接

- 收敛规格：`docs/superpowers/specs/2026-07-17-single-storage-system-convergence-design.md`。
- 详细计划继续使用原 15 项文件，不创建并行储能计划；Task 1-5 保持完成，Task 6 是唯一下一棒。
- Task 10 只扩展原有储能设备页；Task 14 改为现有能耗分析页面的“光储 EMS”工作区。
- Task 15 增加精确设备模拟数据预览/清理与模拟器到厂商网关的适配器替换验收。

## 园区光储 Task 6 交接

- Task 6 已完成储能控制服务、分类回执 dispatcher、旧补偿路径兼容与每分钟超时任务。
- 下一棒为后端储能 Task 7：按 TDD 增加来源/运行标识、设备级自动门禁和原有嵌套储能 API。
- 人工控制必须继续复用 Task 6 服务；自动控制默认关闭，且需要全局与单设备双门禁。
- 真实联调前继续要求显式升级并复核开发库 migration。

## 园区光储 Task 7 交接

- Task 7 已完成 `0003` 增量、来源/运行标识、设备级自动门禁及原有储能嵌套 API。
- 下一棒为 Task 8：纯规则优先，随后以全局/设备双门禁编排 Task 6 命令服务。
- 模拟控制仍默认关闭，且配置 topic 与生产控制 topic 重叠时主动拒绝。
- 实际开发库真实联调前需显式升级到 `20260717_0003` 并复核。

## 园区光储 Task 8 交接

- Task 8 已完成纯规则、双门禁 EMS 服务与条件注册的 60 秒任务。
- 下一棒为 Task 9：模拟器命令执行、稳定运行标识、终态幂等和故障注入。
- Task 9 不得把场景/速度/故障动作接入真实控制 topic，也不得把 accepted 当成功。
- 实际开发库真实联调前仍需显式升级到 `20260717_0003`。

## 园区光储 Task 9 交接

- Task 9 提交：`6254dd8a`；模拟器控制执行、稳定运行标识、终态幂等和固定故障注入已完成。
- 下一棒为前端 Task 10：只增强原有 `StorageMonitorView`，消费现有储能嵌套 API 和控制日志，不新增页面或路由。

## 园区光储 Task 10 交接

- Task 10 提交：`a83cbc49`；统一储能设备工作台、权限门禁、命令时间线与目标/实际功率展示已完成。
- 下一棒为后端 Task 11：固定 PuLP 并按 TDD 实现确定性的 96 时段日前 MILP 优化器。
- Task 11 保持纯领域边界，不提前持久化计划或下发控制；这些属于 Task 12。
- Task 11 不需要新建数据库；真实 MQTT 联调前才需要将开发库显式升级到既有 head `20260717_0003`。

## 园区光储 Task 11 交接

- Task 11 提交：`48b91c90`；确定性日前 MILP 优化器、开源 CBC 依赖和异常边界已完成。
- 下一棒为后端 Task 12：事务持久化 96 时段计划、接入实时 EMS、安全回退、每日任务和三项嵌套 API。
- 优化失败必须保留上一有效计划；计划目标进入控制前仍必须经过 SOC、温度、设备故障、通信和人工接管保护。
- Task 12 不新增 migration，但依赖既有计划表；真实联调前需升级开发库到 `20260717_0003`。

## 园区光储 Task 12 交接

- Task 12 提交：`b10991d5`；计划持久化、实时 EMS 安全执行、每日任务与嵌套 API 已完成。
- 下一棒为后端 Task 13：在 energy 域增加光储总览和同输入基线/规则/日前对比 API。
- 跨策略结果必须携带 `scenario_key`、seed、initial SOC 和输入校验和，所有指标由原始序列计算。
- Task 13 不修改前端；Task 14 才在现有 `/energy` 页面增加光储 EMS 工作区。
- accepted 不能在前端显示为成功；来源、正充负放、安全拒绝、pending 禁用和终态原因必须按现有后端契约展示。
- 实际开发库真实 MQTT 联调前仍需显式升级到 `20260717_0003` 并复核。

# 2026-07-16 后端可靠性阶段 2A 交接快照

> 本文件是切换到园区光储主题时的前一主题交接快照。若 2026-07-16 当天需要记录园区光储主题收尾交接，必须在本文末尾追加独立章节，不得覆盖或改写本快照。

## 当前主题

- 前一主主题：`后端可靠性基线与渐进式解耦治理`。
- 当前阶段：阶段 2A Alembic 链可信化。
- 状态：用户批准后暂停，待恢复；未完成、未验收通过。

## 已知信息

- 事实清单：`docs/plans/backend-reliability-phase2-inventory.md`。
- 原正式 PLAN：`docs/plans/PLAN-20260610-backend-reliability-progressive-decoupling.md`。
- 当前 migration 链的 offline SQL、fresh PostgreSQL、migration-built existing 和 runtime-sync existing 路径均无通过证据。
- offline SQL 在 revision `20260412_0003` 的在线检查逻辑处稳定失败。
- 三类 PostgreSQL migration fixture 尚未建立。

## 恢复后的下一棒

### 规则角色

- 恢复阶段 2A 为主主题，并重新建立唯一的 current-status 与 handoff 主区。
- 确认 revision ID 和历史顺序保持不变，schema 完整性不能只检查 Alembic head。

### 后端角色

- 按获批的阶段 2A 设计与实施计划修复确定性 baseline、offline SQL 和 schema reconciliation。
- 建立 fresh、migration-built existing、runtime-sync existing 三类 PostgreSQL fixture。
- 不用直接 stamp 或 runtime metadata 建表掩盖 schema 漂移。

### 验收角色

- 核对 offline SQL 可完整生成并可执行。
- 核对 fresh 与两类 existing database 升级后数据保留、schema 对齐。
- 核对 CI migration 步骤恢复为 blocking。

## 限制条件

- 暂停期间不修改 migration 或阶段 2A 生产代码。
- 园区光储主题仅允许不触碰数据库的纯领域模型先行。
- 园区光储主题 Task 3 及依赖持久化的任务必须等待本阶段恢复并通过门禁。

## 未解决风险

- 动态 baseline 和 runtime schema sync 仍会造成同 revision 不可复现。
- `storage_telemetry` 等结构存在 ORM metadata 痕迹但缺少正式 migration 承载。
- 部署 migration、readiness 和 rate-limit 语义属于后续阶段，不应在阶段 2A 恢复时顺手扩张。

## 园区光储主题暂停快照

- 园区光储 Task 1 与 Task 2 均已正式完成；Task 2 只实现纯电池领域模型与兼容惰性导出。
- 已保留储能提交至 `efbbe808`：`aeda9166`、`8557fcf9`、`cd64879c`、`efbbe808`。
- Task 3 需要新增持久化模型与 migration，因阶段 2A 的确定性迁移门禁未通过而保持阻塞。
- 暂停期间不得提前修改储能数据库模型或 migration，不得把当前 metadata 中的 `storage_telemetry` 视为正式迁移证据。
- 交接顺序：先由后端完成阶段 2A，再由验收核对静态契约、三路径指纹、启动契约、CI 门禁和开发库重建。
- 精确恢复条件：`20260716_0001` 验收通过；随后主区切回园区光储，Task 3 仅改为可开始，不提前标记完成。
